from tests.app_mock.executors.registry import MOCK_EXECUTORS_CLS_MAPPING

# NOTE: temporary — points at the mock executors under tests/app_mock until
# real domain executors are built, then this should map to those instead.
EXECUTORS_CLS_MAPPING = MOCK_EXECUTORS_CLS_MAPPING
