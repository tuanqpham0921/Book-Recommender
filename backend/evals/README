Currently being ignore by git
need to set up a test commands and structure it better later

has raw .sql data, and is useful for future and keeping track of things

The flow:

1. run the test suites
2. dump both chat_runs and feedback
    * need to dump the console somehow
    * before it overwrites
3. make a personal review.md
    * save chat id, session id, comit id
    * write a report
    * TODO: need a better structure later
    * maybe even a jupyter notebook
4. ask claude to review if you want

!!! Important, save the commit sha or something
!!! so we can reference it back

it should also have basic stats
    * like average token counts, total token counts, etc...
    * how many failed, how many have run-time errors
    * we can build this over time

